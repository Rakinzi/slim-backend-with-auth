<nav class="navbar navbar-expand-md navbar-dark bg-dark">
	<div class="container-fluid">
		{% include "components/Anchor.twig" with {
    text: user ? 'AI City' : 'Welcome',
    attributes: {
        href : user ? url_for('secure.dashboard') : url_for('home'),
        class: 'navbar-brand'
    } 
}%}

		{% include "components/Button.twig" with{
    buttonText: '<span class="navbar-toggler-icon" style="display: block"></span>',
    buttonAttributes: {
        class: 'navbar-toggler',
        type: 'button',
        'data-bs-toggle': 'collapse',
        'data-bs-target': '#navbarNav',
        'aria-controls': 'navbarNav',
        'aria-expanded': 'false',
        'aria-label': 'Toggle navigation'
    }
}%}

		{% if user %}
			<div class="collapse navbar-collapse" id="navbarNav">
				{% set navBarItems = [
            {href: './about', text: 'About'},
            {href: './contact', text: 'Contact'},
            {href: url_for('secure.projects'), text: 'Projects'},
            {href: './profile', text: 'Profile'},
            {href: url_for('secure.logout'), text: 'Logout'}

			] %}

				{% include "components/NavBarList.twig"%}
			{% endif %}
		</div>
	</div>
</nav>
