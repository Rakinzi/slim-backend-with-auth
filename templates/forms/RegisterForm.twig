
	{% include "components/Form.twig" with{
                action: './register',
                inputItems: registerData.inputs,
                method: 'POST',
				error: error.message,
				errorAttributes: error.attr,
				buttonAttributes: registerData.registerButton.attributes,
				buttonText: registerData.registerButton.text,
				linkButtonText: registerData.loginButton.text,
				linkButtonAttributes: registerData.loginButton.attributes
				
    }%}