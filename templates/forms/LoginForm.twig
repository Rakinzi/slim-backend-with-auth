{% include "components/Form.twig" with{
                action: './login',
                inputItems: loginData.inputs,
                method: 'POST',
				error: error.message,
				errorAttributes: error.attr,
				buttonAttributes: loginData.loginButton.attributes ,
				buttonText: loginData.loginButton.text ,
				linkButtonText: loginData.registerButton.text,
				linkButtonAttributes: loginData.registerButton.attributes
    }%}
